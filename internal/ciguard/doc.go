// Package ciguard не содержит продуктового кода и импортировать его неоткуда:
// здесь живёт только сторож (matrix_test.go), сверяющий матрицу ОС в
// .github/workflows/ci.yml с матрицей job build в .github/workflows/release.yml.
//
// Пакет заведён с этим единственным не-тестовым файлом намеренно: каталог без
// единого не-тестового файла ведёт себя в `go build ./...` и `go list ./...`
// иначе, чем обычный пакет, и сторож проще держать в пакете, который эти
// команды видят как все остальные.
package ciguard
