// Package permguard не содержит продуктового кода и импортировать его
// неоткуда: здесь живёт только структурный сторож прав создаваемых файлов
// (permguard_test.go). Пакет заведён с этим единственным не-тестовым файлом
// по той же причине, что и internal/ciguard: каталог без единого
// не-тестового файла ведёт себя в `go build ./...` и `go list ./...` иначе,
// чем обычный пакет.
package permguard
