# --- Секреты и локальные данные пользователя — НИКОГДА не публиковать ---
# Зашифрованные ключи vpn:// и локальное состояние приложения
/Настройки/
# Клиентские конфиги WireGuard (содержат приватные ключи и адрес сервера)
/Конфигурации/
*.conf
*.avlt
# исключение: golden-фикстуры для тестов (внутри фейковый vpn://golden-fixture-key)
!core/testdata/v1.avlt
!core/testdata/v2.avlt
# PR-A4: хранилище со СТАРЫМ КОРОТКИМ пином (10 символов), внутри фейковый
# vpn://oldshortpin-fixture. Нужно затем, чтобы тест тупика перезапечатывания
# компилировался на предыдущей ревизии и краснел там по существу, а не
# ошибкой компиляции.
!core/testdata/shortpin.avlt
throttle.json
ui.json
crash.log

# --- Сборочные артефакты ---
*.exe
*.exe~
*.out
/dist/

# --- Служебное ---
# Рабочие копии агентов (git worktree) и их временные файлы. Внутри лежит
# полная копия дерева на старых ревизиях: при `git add -A` уехала бы в
# историю, а сторожа вроде TestNoInsecureHostKey спотыкались бы о старый код.
.claude/

# --- Прочее ---
.DS_Store
Thumbs.db
